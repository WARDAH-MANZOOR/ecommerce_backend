{
  "name": "upaisa",
  "version": "1.0.0",
  "private": true,
  "description": "",
  "scripts": {
    "start": "nodemon ./src/bin/www",
    "dev": "npx tsc && nodemon ./src/bin/www",
    "build": "npm run migrate && npm run generate && npx tsc",
    "migrate": "prisma migrate dev --schema src/prisma/schema.prisma",
    "generate": "prisma generate --sql --schema src/prisma/schema.prisma",
    "test": "jest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "@types/express": "^5.0.6",
    "@types/node": "^25.0.3",
    "nodemon": "^3.1.11",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
